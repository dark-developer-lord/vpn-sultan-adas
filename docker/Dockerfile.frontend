# Use pre-built Angular app
FROM nginx:alpine

# Copy pre-built Angular app to nginx
COPY frontend/dist/vpn-dashboard/browser /usr/share/nginx/html

# Copy nginx config
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf

# Expose port
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]
